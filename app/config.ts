export const proxy = {
  protocol: "http",
  host: "127.0.0.1",
  port: 7890,
};

export const enableProxy = false;

export const keys = ["leeesovely", "pyoapple"];

export const saveDir = "./images";
